{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account in Keylo.ai, including both registered and guest users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, used for authentication and contact. Null for guest users.",
          "format": "email"
        },
        "authProvider": {
          "type": "string",
          "description": "The authentication provider used by the user (e.g., 'email', 'google', 'apple', 'guest')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        },
        "lastLoginAt": {
          "type": "string",
          "description": "Timestamp of the user's last login or guest session start.",
          "format": "date-time"
        },
        "isGuest": {
          "type": "boolean",
          "description": "Indicates if the user is currently operating as a guest without a registered account."
        },
        "guestSessionExpiresAt": {
          "type": "string",
          "description": "Timestamp when a guest user's temporary session is set to expire, limiting free usage.",
          "format": "date-time"
        },
        "profileId": {
          "type": "string",
          "description": "Reference to the associated UserProfile entity."
        }
      },
      "required": [
        "id",
        "authProvider",
        "createdAt",
        "lastLoginAt",
        "isGuest"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Stores user-specific profile details and preferences.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the associated User entity."
        },
        "displayName": {
          "type": "string",
          "description": "The user's chosen display name for the app."
        },
        "avatarUrl": {
          "type": "string",
          "description": "URL to the user's profile avatar image.",
          "format": "uri"
        },
        "onboardingCompleted": {
          "type": "boolean",
          "description": "Indicates whether the user has completed the initial onboarding flow."
        },
        "preferredConversationStyle": {
          "type": "string",
          "description": "The user's preferred conversational style or mode for the AI companion."
        },
        "interests": {
          "type": "array",
          "description": "A list of topics or interests provided by the user during onboarding.",
          "items": {
            "type": "string"
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of the last update to the user profile.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "onboardingCompleted"
      ]
    },
    "Subscription": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Subscription",
      "type": "object",
      "description": "Manages a user's premium subscription details and status via Stripe.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Subscription entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the associated User entity."
        },
        "stripeCustomerId": {
          "type": "string",
          "description": "The customer ID assigned by Stripe for billing management."
        },
        "stripeSubscriptionId": {
          "type": "string",
          "description": "The subscription ID assigned by Stripe for the active plan."
        },
        "planType": {
          "type": "string",
          "description": "The type of subscription plan (e.g., 'free', 'monthly', 'yearly')."
        },
        "status": {
          "type": "string",
          "description": "The current status of the subscription."
        },
        "currentPeriodEnd": {
          "type": "string",
          "description": "Timestamp when the current billing period ends.",
          "format": "date-time"
        },
        "trialEndsAt": {
          "type": "string",
          "description": "Timestamp when the subscription trial period ends, if applicable.",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the subscription record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of the last update to the subscription record.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "planType",
        "status"
      ]
    },
    "Usage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Usage",
      "type": "object",
      "description": "Tracks a user's message usage for free limits and premium feature access.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Usage entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the associated User entity."
        },
        "messageCountToday": {
          "type": "number",
          "description": "The number of messages sent by the user within the current 24-hour period."
        },
        "messageCountTotal": {
          "type": "number",
          "description": "The total number of messages sent by the user since account creation."
        },
        "freeMessageLimitTotal": {
          "type": "number",
          "description": "The total cumulative message limit for free users."
        },
        "dailyMessageLimit": {
          "type": "number",
          "description": "The daily message limit for free users."
        },
        "lastDailyResetAt": {
          "type": "string",
          "description": "Timestamp of the last daily message count reset.",
          "format": "date-time"
        },
        "isPremiumUser": {
          "type": "boolean",
          "description": "Flag indicating if the user has an active premium subscription."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the usage record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of the last update to the usage record.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "messageCountToday",
        "isPremiumUser"
      ]
    },
    "Conversation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Conversation",
      "type": "object",
      "description": "Represents a single chat session between a user and the AI companion.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Conversation entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who owns this conversation."
        },
        "title": {
          "type": "string",
          "description": "An auto-generated or user-edited title for the conversation."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the conversation was initiated.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of the last message or update within the conversation.",
          "format": "date-time"
        },
        "mode": {
          "type": "string",
          "description": "The operational mode or persona for the AI in this conversation."
        },
        "isArchived": {
          "type": "boolean",
          "description": "Flag indicating if the conversation has been archived."
        },
        "lastMessagePreview": {
          "type": "string",
          "description": "A short snippet of the most recent message."
        }
      },
      "required": [
        "userId",
        "title",
        "createdAt",
        "updatedAt"
      ]
    },
    "Message": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Message",
      "type": "object",
      "description": "Represents an individual message within a conversation.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Message entity."
        },
        "conversationId": {
          "type": "string",
          "description": "Reference to the Conversation this message belongs to (optional for global chat)."
        },
        "senderId": {
          "type": "string",
          "description": "Identifier of the sender (userId or 'model')."
        },
        "role": {
          "type": "string",
          "description": "The role of the sender ('user' or 'model')."
        },
        "content": {
          "type": "string",
          "description": "The text content of the message."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp when the message was sent.",
          "format": "date-time"
        },
        "sentimentScore": {
          "type": "number",
          "description": "Optional numerical score representing the detected sentiment."
        },
        "isLiked": {
          "type": "boolean",
          "description": "Flag indicating if the user liked this specific message."
        }
      },
      "required": [
        "role",
        "content"
      ]
    },
    "AIChatMemory": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AIChatMemory",
      "type": "object",
      "description": "Stores long-term memory and contextual information for the AI companion.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AIChatMemory entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User this memory belongs to."
        },
        "facts": {
          "type": "array",
          "description": "An array of structured facts about the user.",
          "items": {
            "type": "string"
          }
        },
        "preferences": {
          "type": "array",
          "description": "An array of AI's inferred preferences for the user.",
          "items": {
            "type": "string"
          }
        },
        "summaries": {
          "type": "array",
          "description": "An array of summaries of past conversations.",
          "items": {
            "type": "string"
          }
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of the last update to the AI's memory.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "updatedAt"
      ]
    },
    "AppEvent": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AppEvent",
      "type": "object",
      "description": "Records various application events for analytics.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AppEvent entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who triggered the event."
        },
        "eventType": {
          "type": "string",
          "description": "The specific type of event."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp when the event occurred.",
          "format": "date-time"
        },
        "source": {
          "type": "string",
          "description": "The source or origin of the event."
        }
      },
      "required": [
        "eventType",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores core user authentication and session details."
        }
      },
      {
        "path": "users/{userId}/profile/info",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "User-specific profile details."
        }
      },
      {
        "path": "users/{userId}/usage/stats",
        "definition": {
          "entityName": "Usage",
          "schema": {
            "$ref": "#/backend/entities/Usage"
          },
          "description": "Tracks user message usage and premium status."
        }
      },
      {
        "path": "users/{userId}/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": {
            "$ref": "#/backend/entities/Message"
          },
          "description": "Direct chat messages for the user."
        }
      },
      {
        "path": "users/{userId}/conversations/{conversationId}",
        "definition": {
          "entityName": "Conversation",
          "schema": {
            "$ref": "#/backend/entities/Conversation"
          },
          "description": "Archived chat sessions."
        }
      },
      {
        "path": "users/{userId}/aiChatMemory/main",
        "definition": {
          "entityName": "AIChatMemory",
          "schema": {
            "$ref": "#/backend/entities/AIChatMemory"
          },
          "description": "Long-term AI memory for the user."
        }
      }
    ]
  }
}